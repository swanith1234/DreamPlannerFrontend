<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Permissions ──────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required for showing notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required so the BroadcastReceiver can fire even from a killed state -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Required for FOREGROUND_SERVICE if we ever add a foreground worker -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- ── Main Activity ──────────────────────────────────────────────── -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <!-- Default launcher intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep-link intent: notification body tap opens ignitemate://app/... -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ignitemate" android:host="app" />
            </intent-filter>
        </activity>

        <!-- ── Firebase Messaging Service ────────────────────────────────── -->
        <!--
            Handles DATA-ONLY FCM messages (no notification block).
            Receives the message via onMessageReceived() and builds a rich
            local notification with RemoteInput and action buttons.
            android:exported="false" — only FCM system can start this service.
        -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            Remove the @capacitor/push-notifications plugin's own MessagingService
            from the merged manifest.

            WHY: FCM dispatches a message to exactly ONE service registered for
            com.google.firebase.MESSAGING_EVENT — the first match returned by
            PackageManager. With both the plugin's service and ours declared, which
            one wins is not guaranteed. If the plugin's won, our data-only payload
            (no `notification` block) rendered NOTHING at all, because building the
            visible notification is our service's job. That non-determinism is the
            "delivery issues" behind the earlier revert.

            SAFE because token registration does not depend on this service:
            PushNotificationsPlugin.register() calls FirebaseMessaging.getToken()
            directly and fires the JS `registration` event from that callback.

            TRADE-OFF: the plugin's `pushNotificationReceived` JS event and
            onNewToken-driven token rotation no longer fire. We don't use the
            former, and AppShell calls register() on every launch, so a rotated
            token re-syncs on next app start.
        -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />

        <!-- ── Notification Action Broadcast Receiver ─────────────────────── -->
        <!--
            Receives action button taps and inline-reply text from the notification.
            android:exported="false" — only this app can send intents to it.
        -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ignitemate.app.ACTION_PROGRESS" />
                <action android:name="com.ignitemate.app.ACTION_REPLY" />
            </intent-filter>
        </receiver>

        <!-- ── File Provider (Capacitor requirement) ──────────────────────── -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
